pipeline {
  agent any;
  stages {
      stage ('Build') {
        steps {
          echo "This is Build stage"
        }
      }
    stage ('deploy') {
        steps {
          echo "This is Build stage"
        }
      }
stage ('test') {
        steps {
          echo "This is Build stage"
        }
      }

   }
}
